.env
.venv/
# Google Cloud service-account keys — never commit
*-*.json
!*.geojson
venv/
__pycache__/
.ipynb_checkpoints/
.DS_Store

# Rendered terrain overlays (cache, rebuilt on demand)
.overlay_cache/

# IDE
.idea/

# Large binaries that are not project source: slide decks, screenshots and the
# rendered PDFs of drafts. The manuscript itself (4.docx) is tracked.
*.pptx
Screenshot*.png

# Regenerable intermediates, not source. The plate builder re-fetches these
# from the API (scripts/build_paper_figure_plates.py) and the district gallery
# is rebuilt by scripts/district_eval.py; together they are ~60 MB of rasters
# that no reader of this repository needs in its history.
doc/assets/paper_figs/sources/
doc/assets/districts/
doc/paper/*.pdf
doc/assets/confusion_matrices_before_after.*

# LaTeX build artifacts and backups
*.aux
*.tex.bak

# Duplicate figure extraction dump, superseded by figures/
extracted_figures.zip
extracted_figures/

# Claude Code local session config
.claude/

# Loose personal/reference files kept outside the repo (drafts, slide decks,
# guidance docs sent by reviewers) — not project source.
/1571316711 paper.pdf
/Design.pdf
/Geo-Engine Classifier Presentation.pdf
/LULC_05Sep2026_Constructive_Correction_Guide.docx
/Paper.docx
/Paper.pdf
/Research paper formatting guidance.zip
/img1-mn.jpeg
/img2-rp.jpeg
